# ============================================================
# Variaveis de ambiente — Producao (VPS)
# Copie para .env.production.local e preencha os valores reais
# ============================================================

NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://zjkjhzzhpxiakuhloftc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# JWT — gere com: openssl rand -base64 48
JWT_SECRET=

# Tiny ERP — credenciais OAuth2 ficam no banco (tiny_connections)
# Configure via /admin/tiny na interface web

# Flask API — no mesmo servidor, porta 5000
FLASK_API_URL=http://127.0.0.1:5000
FLASK_INTERNAL_API_KEY=

